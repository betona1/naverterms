# =============================================================================
# naverterms 환경변수 예시 파일
# -----------------------------------------------------------------------------
# 이 파일을 `.env` 로 복사한 뒤 실제 값을 채워 넣으세요.
#   cp .env.example .env
#
# ⚠️  실제 .env 파일은 .gitignore 로 제외되어 깃에 올라가지 않습니다.
#     API 키 / 비밀번호 / DB 자격증명을 절대 커밋하지 마세요.
# =============================================================================

# -----------------------------------------------------------------------------
# Django
# -----------------------------------------------------------------------------
# 임의의 긴 랜덤 문자열. 생성 예: python -c "import secrets;print(secrets.token_urlsafe(50))"
DJANGO_SECRET_KEY=

# -----------------------------------------------------------------------------
# DB 1) naverdb — 키워드 / 분석 / 순위 (Django ORM)
# -----------------------------------------------------------------------------
NAVER_DB_HOST=127.0.0.1
NAVER_DB_PORT=3306
NAVER_DB_NAME=naverdb
NAVER_DB_USER=root
NAVER_DB_PASSWORD=

# -----------------------------------------------------------------------------
# DB 2) myproduct — 스마트스토어 상점 / 상품 (Raw SQL)
# -----------------------------------------------------------------------------
MYPRODUCT_DB_HOST=127.0.0.1
MYPRODUCT_DB_PORT=3306
MYPRODUCT_DB_NAME=myproduct
MYPRODUCT_DB_USER=root
MYPRODUCT_DB_PASSWORD=

# -----------------------------------------------------------------------------
# DB 3) owimage — 오너클랜 이미지 DB (선택)
# -----------------------------------------------------------------------------
OWIMAGE_DB_HOST=127.0.0.1
OWIMAGE_DB_PORT=3306
OWIMAGE_DB_NAME=owimage
OWIMAGE_DB_USER=
OWIMAGE_DB_PASSWORD=

# -----------------------------------------------------------------------------
# 네이버 검색 API (순위추적) — https://developers.naver.com/apps
#   "애플리케이션 등록" → 검색 API 사용 설정 후 Client ID / Secret 발급
# -----------------------------------------------------------------------------
NAVER_SEARCH_CLIENT_ID=
NAVER_SEARCH_CLIENT_SECRET=

# -----------------------------------------------------------------------------
# 네이버 검색광고 API (연관키워드) — https://manage.searchad.naver.com
#   도구 → API 사용 관리 에서 발급
#   CUSTOMER_ID = 계정 우상단 고객 ID(숫자)
# -----------------------------------------------------------------------------
NAVER_AD_CUSTOMER_ID=
NAVER_AD_ACCESS_KEY=
NAVER_AD_SECRET_KEY=

# -----------------------------------------------------------------------------
# 네이버 커머스 API (스마트스토어 상품) 자격증명은 .env 가 아니라
# myproduct DB 의 smartstoreIdList 테이블(상점별 commerce_api_key /
# commerce_secret_key 컬럼)에 저장됩니다. 프론트의 [상점설정] 모달에서 입력하세요.
#   발급: https://apicenter.commerce.naver.com — 판매자센터 API 신청
# -----------------------------------------------------------------------------

# -----------------------------------------------------------------------------
# 텔레그램 알림 (선택) — @BotFather 로 봇 생성 후 토큰/챗ID 입력
# -----------------------------------------------------------------------------
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# -----------------------------------------------------------------------------
# SMS 발송 (선택)
# -----------------------------------------------------------------------------
SMS_ADMIN_URL=
SMS_ADMIN_KEY=
SMS_ADMIN_DEFAULT_SENDER=

# -----------------------------------------------------------------------------
# 이미지 AI 서비스 / 미디어 (선택)
# -----------------------------------------------------------------------------
IMAGE_AI_BASE_URL=http://localhost:8902
PUBLIC_MEDIA_BASE_URL=
